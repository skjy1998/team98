export interface Playertype {
  id: string;
  name: string;
  position: string;
  number: number;
  birth: string;
  appearance: number;
  goal: number;
}

export const playerPositions = ["FW", "MF", "DF", "GK"];
